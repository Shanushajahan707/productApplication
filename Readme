# 🧾 Backend - Product Management System

A simple Node.js, Express.js, and MongoDB backend for handling:

- User authentication (JWT access & refresh tokens)
- OTP verification (via Nodemailer)
- Profile management (Able to block another users to prevent seeing our profile data)
- Blocking system
- Brand and Product management (with filtering/sorting)

---

## 🚀 Stack
- Node.js + Express.js
- MongoDB + Mongoose
- TypeScript
- JWT (access & refresh tokens)
- bcryptjs (password hashing)
- nodemailer (OTP email verification)

---

## ▶️ Run Locally

1. Clone the repo:
```bash
git clone https://github.com/Shanushajahan707/productApplication.git
cd backend
```

2. Install dependencies:
```bash
npm install
```

3. Create `.env` file:
```
PORT=5000
MONGODB_URI=<your-mongo-uri>
JWT_SECRET=<jwt-secret>
REFRESH_SECRET=<refresh-token-secret>
EMAIL_USER=<your-email>
EMAIL_PASS=<your-email-password>
```

4. Start the server:
```bash
npm run dev
```

---

## 🙌 Author
Shanu Shajahan  
MEAN Stack Developer, Kerala

